import React from "react";
import { AlertDialog as NBAlertDialog } from "native-base";
export const AlertDialogFooter = ({ ...props }: any) => {
  return <NBAlertDialog.Footer {...props}></NBAlertDialog.Footer>;
};
