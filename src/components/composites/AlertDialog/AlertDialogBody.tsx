import React from "react";
import { AlertDialog as NBAlertDialog } from "native-base";
export const AlertDialogBody = ({ ...props }: any) => {
  return <NBAlertDialog.Body {...props}></NBAlertDialog.Body>;
};
