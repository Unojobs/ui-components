import React from "react";
import { FormControl as NBFormControl } from "native-base";
export const FormControlHelperText = ({ ...props }: any) => {
  return <NBFormControl.HelperText {...props}></NBFormControl.HelperText>;
};
