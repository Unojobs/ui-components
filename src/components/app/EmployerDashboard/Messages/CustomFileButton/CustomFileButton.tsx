import React from 'react';
import { VStack } from '../../../../primitives';
import type { CustomFileButtonProps } from './types';

export const CustomFileButton = (_props: CustomFileButtonProps) => {
  return <VStack />;
};
