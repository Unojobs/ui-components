import React from 'react';
import { HStack } from '../../../../primitives';
import type { TLinkPreviewProps } from './types';

export const LinkPreview = (_props: TLinkPreviewProps) => {
  return <HStack />;
};
