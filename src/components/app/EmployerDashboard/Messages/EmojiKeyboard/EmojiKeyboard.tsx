import React from 'react';
import { HStack } from '../../../../primitives';
import type { IEmojiKeyboardProps } from './types';
export const EmojiKeyboard = (_props: IEmojiKeyboardProps) => {
  return <HStack />;
};
