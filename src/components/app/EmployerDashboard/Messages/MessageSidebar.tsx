// message screen sidebar with title+newMessageBtn + dropdown + searchbar + CardList
import React from 'react';
export interface MessageSidebarProps {}
export const MessageSidebar = () => {
  return <p>Message Sidebar</p>;
};
